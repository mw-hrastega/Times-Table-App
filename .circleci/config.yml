version: 2.1
orbs:
  matlab: mathworks/matlab@1
jobs:
  my-job:    
    machine:
      image: ubuntu-2204:current
    steps:
      - checkout
      - matlab/install
      - matlab/run-tests:
          test-results-junit: test-results/results.xml
          code-coverage-html: code-coverage/coverage.html
      - store_test_results:
          path: test-results
workflows:
  test:
    jobs:
      - my-job
      
