name: Run MATLAB Commands
on: [workflow_dispatch]

jobs:
  my-job:
    name: Say hello from MATLAB
    runs-on: self-hosted
    steps:
      - name: Run MATLAB from the system shell
        run: matlab -batch "disp('hello world'),matlabroot"
  
  
